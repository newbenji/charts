podTemplate(containers: [
    containerTemplate(name: 'helm', image: 'alpine/helm:3.1.1', command: 'sleep', args: '99d')
  ]) {

    node(POD_LABEL) {

        stage('Test helm') {
            container('helm') {
                stage('helm version') {
                    sh '''
                    helm version
                    ls -al
                    pwd
                    '''
                }
            }
        }

    }
}
