apiVersion: v1
kind: ConfigMap
metadata:
  name: config-blueprints
  labels:
    {{- include "home-assistant.labels" . | nindent 4 }}
data:
  scripts.yaml: | {{ include "home-assistant.blueprints" . | nindent 4 }}
