name: actions-test
on:
  push:
    branches-ignore:
      - main
  # pull_request:
  #   branches:
  #     - test-branch
  # schedule:
    # - cron: '* * * *'
jobs:
  call-workflow-passing-data:
    # uses: "newbenji/charts/.github/workflows/reused.yaml@${{ github.head_ref || github.ref_name }}"
    uses: newbenji/charts/.github/workflows/reused.yaml@esp32home

    with:
      config-path: .github/labeler.yml
    secrets: inherit


  print-hello:
    runs-on: ${{ vars.RUNNER }}
    steps:
      - run: echo "Hello, world"
