name: actions-test
on:
  push:
    branches-ignore:
      - main
  # pull_request:
  #   branches:
  #     - test-branch
  # schedule:
    # - cron: '* * * *'
jobs:
  print-hello:
    runs-on: ${{ vars.RUNNER }}
    steps:
      - run: echo "Hello, world"
