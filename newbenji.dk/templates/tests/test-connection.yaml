apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "newbenji.dk.fullname" . }}-test-connection"
  labels:
    {{- include "newbenji.dk.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "newbenji.dk.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
