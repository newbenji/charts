apiVersion: v1
kind: Service
metadata:
  name: {{ include "newbenji.dk.fullname" . }}
  labels:
    {{- include "newbenji.dk.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "newbenji.dk.selectorLabels" . | nindent 4 }}
